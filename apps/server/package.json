{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:create": "wrangler d1 create battleships",
    "db:migrate": "wrangler d1 execute battleships --remote --file=./db/schema.sql",
    "db:migrate:local": "wrangler d1 execute battleships --local --file=./db/schema.sql",
    "check": "biome check --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "game-messages": "workspace:",
    "game-rules": "workspace:",
    "hono": "^4.7.11",
    "ramda": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@cloudflare/workers-types": "^4.20250620.0",
    "@types/node": "catalog:",
    "@types/ramda": "catalog:",
    "typescript": "catalog:",
    "wrangler": "^4.23.0"
  }
}
